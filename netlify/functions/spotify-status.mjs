const ck=h=>Object.fromEntries((h||"").split(";").map(x=>x.trim().split("=")).filter(x=>x.length===2));export default async req=>{try{const v=ck(req.headers.get("cookie")).kina_spotify_session;if(!v)return Response.json({connected:false});const s=JSON.parse(Buffer.from(v,"base64url").toString());return Response.json(s.exp>Date.now()?{connected:true,display_name:s.display_name,account_id:s.account_id}:{connected:false})}catch{return Response.json({connected:false})}};